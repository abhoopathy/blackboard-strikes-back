.page.calendar
    - for (var i=1 ; i <= startsOn ; i++)
        .cal-cell
    - for (var i=1 ; i <= days ; i++)
        .cal-cell(data-day=i)= i
    - for (var i=1 ; i <= (35-startsOn-days) ; i++)
        .cal-cell
